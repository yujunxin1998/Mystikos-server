# 本地开发：Mailpit 捕获邮件，不会投递到真实邮箱。
spring:
  mail:
    host: ${MAIL_SMTP_HOST:localhost}
    port: ${MAIL_SMTP_PORT:1025}
    username: ${MAIL_SMTP_USERNAME:}
    password: ${MAIL_SMTP_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

mystikos:
  mail:
    enabled: true
    from: ${MAIL_FROM:no-reply@mystikos.local}
  sms:
    aliyun:
      # 默认 false，避免本地随手测试产生真实短信费用。
      enabled: ${ALIYUN_SMS_ENABLED:false}
      endpoint: ${ALIYUN_SMS_ENDPOINT:dysmsapi.aliyuncs.com}
      access-key-id: ${ALIBABA_CLOUD_ACCESS_KEY_ID:}
      access-key-secret: ${ALIBABA_CLOUD_ACCESS_KEY_SECRET:}
      sign-name: ${ALIYUN_SMS_SIGN_NAME:}
      verification-template-code: ${ALIYUN_SMS_TEMPLATE_CODE:}
