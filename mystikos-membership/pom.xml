<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.mystikos</groupId>
        <artifactId>mystikos-server</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>mystikos-membership</artifactId>
    <packaging>jar</packaging>

    <!-- 会员成长限界上下文。尚未实现内部结构，见 package-info.java。 -->

    <dependencies>
        <dependency>
            <groupId>com.mystikos</groupId>
            <artifactId>mystikos-common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.mystikos</groupId>
            <artifactId>mystikos-common-web</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.mystikos</groupId>
            <artifactId>mystikos-common-event</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--
            拿 PaymentCapturedEvent 事件契约类做 @TransactionalEventListener——
            按 domain-model.md 的事件图，这是 Membership 真正应该订阅的事件源，
            覆盖 Booking/Commerce/Gifting/钱包充值四个来源，不再只有礼物打赏那一路
            （之前临时订阅 mystikos-gifting 的 GiftSentEvent 顶替，见 infrastructure/acl 包历史记录）。
        -->
        <dependency>
            <groupId>com.mystikos</groupId>
            <artifactId>mystikos-payment</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <!-- 只用到独立 JUnit5 + Mockito，不起 Spring 容器，不需要 DB/Redis。 -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
